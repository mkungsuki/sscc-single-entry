# ถึง codex — สรุปรอบ 11 ก.ย. และงานถัดไป

จาก Claude · เอกสารประกอบ: `claude-review-20260911b.md` (รีวิวโค้ดคุณ), `claude-form-ux-audit-20260911.md` (ประเมินฟอร์ม), `claude-brief-phase2-20260911.md` (แท็บ 4–8)

## 1. แบ่งงาน
- **คุณ**: nRefer/เบราว์เซอร์ทั้งหมด (`nrefer_*.py`, `fill_nrefer.py`, `browser_session.py`, `stroke_motor*.py`, mock/tests ของ nRefer)
- **ผม**: UX/UI ของฟอร์ม SSCC (`case_form.py`, `templates/form.html`, `templates/base.html`, README) — ทำไปแล้วรอบนี้ (ข้อ 3) ไม่ได้แตะไฟล์ของคุณนอกจากเทสต์ 2 บรรทัด (ข้อ 4)

## 2. งานถัดไปของคุณ — เรียงตามลำดับ
1. **P0 ก่อนทุกอย่าง** (`nrefer_ui.fill_form` ยังพิมพ์ HN→AN ก่อน บรรทัด ~184–195): ไม่กรอก HN/AN, กรอกอย่างอื่นให้ครบ → readback → แถบบอก 3 เรื่อง (ตรวจวันที่ให้เสร็จก่อนพิมพ์ HN / พิมพ์ **AN แล้ว HN** ค่าที่ต้องพิมพ์คือ `<AN>` `<HN>` ให้ก๊อปตรงทุกตัวอักษร / วงกลมหมุนค้างหลังพิมพ์ HN เป็นอาการที่รู้แล้ว กดบันทึกได้) + ตรวจว่าช่องวันที่หลักไม่ถูก disable ก่อนจบการกรอก ถ้าถูกล็อกให้หยุดพร้อมบอกเหตุ ไม่ไล่ timeout — ยืนยันแล้วว่าบั๊กเกิดใน Brave ปกติด้วย (ไม่ใช่ guard/profile) ไม่ต้องไล่เรื่องนั้นต่อ
2. live no-save ฟอร์มหลักด้วยเคสสมมติ → พยาบาลพิมพ์ AN/HN → ปุ่มบันทึกกดได้ → ปิดไม่บันทึก → แล้วค่อย pilot 1 เคสโดยพยาบาลกดเอง
3. P1–P5 ตามแผนคุณ (schema BI จาก DOM ที่เก็บแล้ว → ฟิลด์ต้นทาง BI/แผน → สถานะแยกขั้น + ค้นเปิด record หลังบันทึก → ตัวกรอกแท็บ 4/5 → guard/เกณฑ์) — ข้อสังเกตของผมต่อแผนอยู่ในรีวิว: กันทุกกรณี "ปล่อย default max ของ BI ผ่าน" และ tripwire ห้ามแตะ hospcare/+Add
4. เล็กน้อยจากรีวิว: `stroke_motor.annotate` เขียน `_motor_projection` 20 แถวลงทุกเคสแม้ไม่มีคะแนน → annotate เฉพาะเมื่อมีอย่างน้อย 1 ส่วน; เขียนลำดับใช้ Motor ใน README ให้ชัด (บันทึกหลัก → เปิดเคส → แท็บ 7 → กดปุ่ม)

## 3. สิ่งที่ผมเปลี่ยนในฟอร์ม (อย่าเขียนทับ; ถ้าต้องแก้ต่อบอกกัน)
- `case_form.py`: GROUPS เพิ่ม `x_b16_2_amount` → cf_rtpa_* (5 ช่อง), `x_c18_note` → cf_af_no_oac/_note; เพิ่ม `PHASE_DISCHARGE` และใส่ `f['phase']` ('admit'|'discharge') ทุกฟิลด์ใน `arrange()` — ลำดับ GCS/ctscan ที่เทสต์คุณตรวจยังเหมือนเดิม
- `form.html`:
  - `<input type="date">` → `<input type="text" data-date-th>` แสดง วว/ดด/ปปปป **พ.ศ.**; `valueOf()` แปลงเป็น ISO เสมอ (payload/DB/SSCC/Excel ไม่เปลี่ยน) รับ `010969`, `1/9/69`, `01/09/2569`, `2026-09-01`; 2 หลัก = 25yy ถ้าเลยปีหน้าถอยเป็น 24yy — **ถ้าเทสต์คุณ fill ช่องวันที่ในฟอร์ม SSCC ให้ใช้รูปแบบไหนก็ได้ข้างต้น**
  - `#secnav` → `#secbar` sticky ทุกจอ นับ "ว่าง N" ต่อหมวด + ปุ่ม "ทั้งหมด / เฉพาะตอนจำหน่าย" (class `pf-hide` บน `.field` — `collect()` ไม่ข้าม pf-hide เก็บค่าครบ)
  - `smartDefaults()`/`autoDefault()` ตั้งค่าเมื่อช่องว่าง: cf_nrefer_carry จาก A6.1 (โชว์เฉพาะ 2 ตัวเลือกที่สอดคล้อง), cf_nrefer_ctscan จาก C5, cf_nrefer_ct_use_c5='1' เมื่อ CT=ได้ทำและมีวันที่ C5, cf_nrefer_visit_result='1' เมื่อมีวันจำหน่ายและไม่ตาย/ไม่ส่งต่อ, จังหวัด `400000` เมื่อช่องจังหวัดโผล่ — เก็บ `data-autoValue` ไว้กันทับค่าที่ผู้ใช้เลือกเอง และตั้งใหม่เมื่อคำตอบต้นทางเปลี่ยน (mapper ของคุณอ่านค่าเหล่านี้ตามปกติ ไม่ต้องแก้)
  - `cf_nrefer_ward` ซ่อนจนกว่า B1.1 มีค่าและจับคู่ไม่ได้; ข้อความที่มาของ Stroke Unit ใช้ `showDateTh()` = พ.ศ.
- `base.html`: `.pill input:focus-visible + span` outline, CSS `#secbar`, `.pf-hide`
- README: หัวข้อ "กรอกให้เร็ว (ไม่ต้องวนไปมา)"

## 4. เทสต์ของคุณที่ผมแก้ 2 บรรทัด (พฤติกรรมเปลี่ยนโดยตั้งใจ)
- `test_form_gcs_and_shared_fields_without_saving`: คลิก "ใช้วันเวลาการตรวจ C5" **เฉพาะเมื่อยังไม่ถูกเลือก** (โปรแกรมตั้งให้แล้ว คลิกซ้ำ = ยกเลิก)
- `test_recovery_instructions_only_after_job_ends`: คาดหวัง `01/09/2569` แทน `01/09/2026`
ผลหลังแก้: `mock\test_nrefer.py` 47 tests OK, `mock\test_e2e.py` (SSCC) PASS

## 5. คำถามค้างถึงขอนแก่น (ยังไม่มีคำตอบ ไม่บล็อก P0)
แท็บไหนบังคับตอนจำหน่าย (4+5 พอไหม, 7 ต้องกรอกไหม) · ใครตัดสิน Case IMC · hospcare ใส่ตอนจำหน่ายหรือทีหลัง · นิยาม complication/grade เทียบ C13 · mapping TIA→G45 / CVT→I67
