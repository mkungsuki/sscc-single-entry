# Live Edge inspection ledger — 10 September 2026

All runs use `app/mock/live_nrefer_inspection.py`: synthetic HN/AN, blank citizen ID, isolated temporary database/profile and the actual worker with --no-save. No Save click was requested or performed by the agent.

1. First worker run: ThaID login recognized, then TimeoutError. Original logging did not locate the failing stage.
2. Added stage and blocked-endpoint diagnostics. Second run: login recognized, blocked at waiting for Add new. User reported API expire and logout. Guard had blocked POST user/user-status, user/user-by-key and admin/thaid/authenticated plus reference GET requests. This demonstrates a guard compatibility defect; it does not explain the earlier TS027.
3. Inspected 38 public JS bundles without cookies or API calls. `chunk-EGPMWWUF.js` defines getUserStatus/searchUser as POST reads; `chunk-T5BZUTSZ.js` defines thaIDAuthenticated as POST status lookup; `chunk-TO245DYV.js` defines evaluate-choice, dmis-type, visit-result, service-status and modified-rankin-scale as GET reference lookups. Guard now allows exact methods/paths, still denies unknown endpoints and patient/person save/delete. Local regression suite: 27 tests passed, including method/origin restrictions and Save blocked before local server.
4. Third run opened login; user supplied screenshot showing `Forbidden (check your clock)` at /#/login. Worker later reported TargetClosedError; it did not report successful login. Therefore this run does not validate the guard fix end to end.
5. Read-only time measurements: Windows timezone SE Asia Standard Time; Windows Time service stopped. Public HTTP Date ~3 seconds ahead. `w32tm /stripchart /computer:time.windows.com /samples:3 /dataonly`: +2.5121, +2.5138, +2.5131 seconds. Offset is measured; server rejection tolerance and causality remain unproven.
6. Attempt to start W32Time for normal resync failed due to insufficient Windows service privileges (not an automatic approval rejection). Opened Windows Date & Time settings. After the user pressed Sync now, measured offsets at 22:36 were -0.0009242, +0.0015772, +0.0011889 seconds: synchronization succeeded. W32Time status query still reports stopped, so that status alone must not be treated as proof that a user-triggered sync failed.
7. Fresh isolated Edge inspection after clock correction: login succeeded, Add new opened, actual Python worker completed filling and readback. Warnings: inferred Stroke Unit admission time, GCS source-total reminder, Ward fill/readback failure, HN readback failure. No Save click. Clock correction precedes successful login but is not proof of the server's precise rejection mechanism.
8. Public `chunk-EGPMWWUF.js` identifies `libWard(hospcode, searchText)` as POST `/libs/lib-ward`; added that exact read endpoint to guard, retaining denial of `/libs/save-lib-ward`. Current open worker uses the previous source copy, so this fix requires another run. Asked user to read synthetic HN and whether the Ward list is empty because CUA inventory cannot access the running Edge window. HN cause is not yet known; public input binding has no maxlength and person lookup alone does not clear HN. Do not change HN handling on speculation.
9. User screenshot confirms empty HN and empty Ward options in that run; admission/discharge, BP, GCS 3/4/5=12, rt-PA, CT and Stroke Unit values were present. Added optional value-free identity diagnostics (matching/empty/changed, never identifier contents) after filling HN/AN, waiting for HIS, Dx, each text field and each date.
10. Next actual Edge run with ward-read exception and diagnostics: login succeeded; HN/AN matched after initial fill, HIS quiet, Dx, all text and all date steps. Final readback produced **no field failure warnings**, including HN, Ward, dates, mRS and readonly GCS total. Only mapping reminders remained (inferred Stroke Unit admission time and manual review of source GCS total). Synthetic no-save filling has now completed successfully through the real Python worker. No Save action was performed. Prior empty HN did not reproduce, so its cause is unresolved and no speculative identity overwrite was added. Local suite after ward exception: 27 tests passed.

Sources: https://nrefer.moph.go.th/beta/chunk-EGPMWWUF.js, https://nrefer.moph.go.th/beta/chunk-T5BZUTSZ.js, https://nrefer.moph.go.th/beta/chunk-TO245DYV.js (public client code establishes usage; backend implementations are not available).

Do not declare production acceptance or the clock issue fixed from mock tests or the error wording alone.
